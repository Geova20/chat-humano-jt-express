# Valores para rodar localmente
SECRET_KEY=troque-esta-chave-agora

# Use UM dos dois abaixo:

# 1) SQLite local (default)
# (deixe em branco para usar src/database/app.db)
# SQLITE_PATH=/caminho/absoluto/para/app.db

# 2) Postgres local (ou remoto)
# DATABASE_URL=postgresql+psycopg2://usuario:senha@host:5432/banco
